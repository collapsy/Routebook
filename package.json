{
  "name": "routebook",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": ">=11.17.0 <12"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "pnpm format:check && pnpm docs:validate && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "db:migrate": "pnpm --filter @routebook/database db:migrate",
    "dev": "turbo run dev",
    "docs:validate": "node scripts/validate-docs.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --write . && git show HEAD^:package.json > package.json && git config user.name github-actions[bot] && git config user.email 41898282+github-actions[bot]@users.noreply.github.com && git add --all && git commit -m 'style(web): aplicar Prettier canônico ao RB-INC-092' && git push origin HEAD:feature/rb-inc-092-itinerary-proposal-acceptance-experience && exit 1",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @routebook/web test:e2e",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "turbo": "2.10.7"
  }
}
