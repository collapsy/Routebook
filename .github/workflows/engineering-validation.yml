name: Engineering Validation

on:
  pull_request:
    paths:
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".node-version"
      - ".npmrc"
      - ".prettierignore"
      - "prettier.config.mjs"
      - "apps/**"
      - "modules/**"
      - "packages/**"
      - "scripts/**"
      - "docs/**"
      - ".github/workflows/engineering-validation.yml"
  push:
    branches:
      - main
    paths:
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".node-version"
      - ".npmrc"
      - ".prettierignore"
      - "prettier.config.mjs"
      - "apps/**"
      - "modules/**"
      - "packages/**"
      - "scripts/**"
      - "docs/**"
      - ".github/workflows/engineering-validation.yml"

permissions:
  contents: read

concurrency:
  group: engineering-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate monorepo
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DATABASE_URL: postgresql://routebook:routebook@127.0.0.1:5432/routebook

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: routebook
          POSTGRES_USER: routebook
          POSTGRES_PASSWORD: routebook
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U routebook -d routebook"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      - name: Validate documentation
        run: pnpm docs:validate

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Apply database migrations
        run: pnpm db:migrate

      - name: Run component and domain tests
        run: pnpm test

      - name: Smoke development server
        shell: bash
        run: |
          pnpm --filter @routebook/web exec next dev --port 3100 > /tmp/routebook-dev.log 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT

          for attempt in {1..30}; do
            if curl --fail --silent --show-error http://127.0.0.1:3100/viagens > /dev/null; then
              exit 0
            fi
            sleep 1
          done

          cat /tmp/routebook-dev.log
          exit 1

      - name: Build application
        run: pnpm build

      - name: Install Playwright Chromium
        run: pnpm --filter @routebook/web exec playwright install --with-deps chromium

      - name: Run responsive persistence tests
        run: pnpm test:e2e
